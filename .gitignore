node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env*
!.env.example
next-env.d.ts
tsconfig.tsbuildinfo
# public/images IS committed. The Figma URLs in figma-assets.json expire, and
# several assets (logo, hero carousel) never came from Figma at all — leaving
# these untracked means CI/Vercel builds ship a site with no images.
.vercel
.env*.local
.oauth-tmp/
.figma-tmp/
